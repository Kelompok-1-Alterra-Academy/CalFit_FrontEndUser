import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() => ({
  card: {
    backgroundColor: "#84B0BB",
    color: "white",
    cursor: "pointer",
    margin: "10px 0px",
    borderRadius: "1rem",
  },
  cardMedia: {
    padding: "0.5rem",
    height: "194px",
    borderRadius: "1rem",
  },
}));
